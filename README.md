# HTML5-canvas-pixelado
Cómo utilizar CSS para crear un canvas de HTML5 pixelado para juegos Retro de tipo pixel art

Nota: 
antes de dibujar un sprite, ponemos la instrucción:
ctx.imageSmoothingEnabled = false;
